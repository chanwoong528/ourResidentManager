/**
 * @returns randomly generated value to be used for client-server socket interactions.
 */
module.exports.generateKey = function() {
  var passKey = '';
  // TODO: generate a passKey with random combination of numbers and letters?
  // HOTP / TOTP
  // May have to think about encryption later on.

  return passKey;
}
